# Implementation Log

## Commits (Chronological)

### WORKTREE 2026-04-12 — Headless audit/repair provider fallback
- **What**: Updated `scripts/run_agent.sh` so downstream audit/repair runs use `codex exec` with explicit medium reasoning first, then Kilo headless, then Copilot CLI as the final fallback. Aligned the workspace/package instructions, execution-harness plan, promptfile, and `scafforge-context.json` with the same provider order and host-skill freshness rules.
- **Why**: The live operating prompt changed the required host order. Scafforge still described a Codex-only or Codex/Copilot path in key places, which made the executable runner and the written contract disagree.
- **Evidence**: `scripts/run_agent.sh` was still building only Codex commands for `--audit` and `--repair`, while the governing prompt now requires `Codex -> Kilo -> Copilot` and explicit medium reasoning for Codex.
- **Validates**: Headless audit/repair routing now matches the current host policy and can degrade gracefully when provider availability changes.

### WORKTREE 2026-04-12 — Package-root-safe audit/repair skill commands
- **What**: Updated `skills/scafforge-audit/SKILL.md` and `skills/scafforge-repair/SKILL.md` so their published commands work from the Scafforge package root, then updated `scripts/validate_scafforge_contract.py` to enforce the new repair command strings instead of the old skill-directory-relative ones.
- **Why**: A real downstream audit recovered from the wrong documented command path on its own, which proved the skill text and contract validator were both out of sync with the actual execution context used by the headless runner.
- **Evidence**: The `womanvshorseVA` audit initially attempted `python3 scripts/audit_repo_process.py ...` from the package root and failed, then succeeded with `python3 skills/scafforge-audit/scripts/audit_repo_process.py ...`.
- **Validates**: Host-run audits and repairs now have package-root-safe documented commands, and contract validation enforces the same surface.

### WORKTREE 2026-04-12 — GPTTalker remediation-loop hardening
- **What**: Patched `skills/ticket-pack-builder/scripts/apply_remediation_follow_up.py` so an active remediation ticket can never rebuild itself as its own `source_ticket_id`, and so stale candidate-only temp-surface remediation tickets are superseded on later clean runs. Patched `skills/scafforge-audit/scripts/audit_reporting.py` so remediation recommendation IDs reuse only matching unresolved tickets and otherwise allocate the next free `REMED-*` id instead of renumbering by finding order. Patched `skills/scafforge-repair/scripts/run_managed_repair.py` so post-repair verification findings that only point at `/tmp/scafforge-repair-candidate-...` surfaces are recorded for debugging but do not mint persistent subject-repo remediation tickets.
- **Why**: The GPTTalker managed-repair loop had narrowed to a Scafforge-owned defect cluster: `REMED-007` could self-parent and retain stale superseded children (`WFLOW019`), audit recommendation IDs could collide with unrelated existing remediation tickets when finding order changed, and repair verification could promote candidate-only `EXEC001` import failures into persistent repo tickets even when fresh public audits showed no such current-state failure.
- **Evidence**: Before the fix, `GPTTalker/tickets/manifest.json` showed `REMED-007` with `source_ticket_id = REMED-007` and stale follow-up lineage; fresh audit `GPTTalker/diagnosis/20260412-023107/` still produced `WFLOW019`; repair-generated diagnosis `GPTTalker/diagnosis/20260412-024105/` emitted `EXEC001` only from `/tmp/scafforge-repair-candidate-.../candidate/src`; and the old audit recommendation allocator assigned `WFLOW019` to `REMED-009`, colliding with an existing `EXEC001` remediation ticket.
- **Validates**: Fresh public GPTTalker audit `diagnosis/20260412-025410/` now shows only the expected 8 × `EXEC-REMED-001` source-follow-up findings. `WFLOW019`, `CYCLE002`, and public-audit `EXEC001` are absent, while stale temp-surface `REMED-009` / `REMED-010` are superseded instead of lingering open.

### WORKTREE 2026-04-12 — Spinner / womanvshorseVA workflow confirmation on the hardened package
- **What**: Applied the same Scafforge-managed repair loop to `spinner` using the new workflow-selection sync, remediation self-parenting fix, stable remediation-id allocation, and candidate-only follow-up filtering. Then ran fresh public audits on both `spinner` and `womanvshorseVA` against the hardened package.
- **Why**: `GPTTalker` alone was not enough proof; the package changes had to demonstrate that they generalize to another workflow-drift repro repo and to a previously weak repo whose old `WFLOW019` signal might already be obsolete.
- **Evidence**: Fresh `spinner` audit `diagnosis/20260412-025652/` still showed `WFLOW010` + `WFLOW019`; after managed repair, post-repair diagnosis `20260412-030056/` and public audit `20260412-030215/` both dropped to 19 × `EXEC-REMED-001` only. Fresh `womanvshorseVA` audit `diagnosis/20260412-025704/` showed only 2 × `EXEC-REMED-001`, with no surviving `WFLOW019`.
- **Validates**: The current Scafforge package clears the ticket/workflow drift class on at least two independent downstream repos (`GPTTalker`, `spinner`) and no longer reproduces the prior `WFLOW019` surface on `womanvshorseVA`.

### WORKTREE 2026-04-12 — Auto fallback control-flow repair + remaining workflow-drift confirmation
- **What**: Patched `scripts/run_agent.sh` so the auto host-provider loop preserves real provider exit codes and can continue through fallback-eligible failures even under `set -e`. Then re-ran public audits through the repaired auto lane with Codex quota intentionally exhausted, which forced real Kilo fallback execution. In parallel, applied the current Scafforge managed-repair package to the remaining workflow-drift repro repos (`glitch`, `womanvshorseVB`, `womanvshorseVC`, `womanvshorseVD`) and re-audited them.
- **Why**: Once Codex quota expired, the runner regressed from “Codex → Kilo → Copilot” to “Codex only, then `Resolved provider: none`”, which would have broken the rest of the autonomous loop exactly when the fallback policy mattered most. The remaining repos also needed proof that the new ticket-graph/workflow fixes generalized beyond GPTTalker and spinner.
- **Evidence**: The broken trace in `agent-logs/wvhvb-audit-2026-04-12T04-14-20.log` showed `run_codex_exec` returning fallback code `125`, then `run_provider_exec` collapsing that to success and stopping before Kilo. After the patch, `agent-logs/wvhvb-audit-2026-04-12T04-15-11.log`, `glitch-audit-2026-04-12T04-16-27.log`, `wvhvc-audit-2026-04-12T04-16-27.log`, and `wvhvd-audit-2026-04-12T04-16-27.log` all show real `codex` rate-limit failure followed by successful `kilo` execution. Latest public diagnoses show: `womanvshorseVB` = 3 × `EXEC-REMED-001`; `womanvshorseVC` = `EXEC-GODOT-004`, `EXEC-GODOT-005a`, 3 × `EXEC-REMED-001`; `womanvshorseVD` = `EXEC-GODOT-002`, `EXEC-GODOT-005a`, `REF-001`, 2 × `EXEC-REMED-001`; `glitch` = `EXEC-GODOT-002`, `EXEC-GODOT-005b`, `REF-001`, 27 × `EXEC-REMED-001`.
- **Validates**: The host runner now honors the required `Codex -> Kilo -> Copilot` order under real Codex exhaustion, and the current Scafforge workflow package clears the surviving `WFLOW019` / `WFLOW012` / `WFLOW024` / `SKILL002` class from all audited downstream repos.

### WORKTREE 2026-04-12 — False-finish smoke contradiction hardening
- **What**: Patched the repo-template `smoke_test.ts` so Godot export stderr parse/load failures are classified as real failures even when the export command exits `0`. Patched repo-template `workflow.ts` so closeout and repo-readiness claims reject a current smoke-test artifact whose own command record contradicts its PASS verdict. Patched `skills/scafforge-audit/scripts/audit_execution_surfaces.py` so public audit now emits `EXEC-GODOT-006` when a Godot release or finish-validation smoke artifact claims PASS while recording parse/script-load failures or other command-level failure markers. Updated `scripts/smoke_test_scafforge.py` to lock in the stricter behavior.
- **Why**: `womanvshorseVA` proved Scafforge could still accept a false-finish release: the current `RELEASE-001` smoke artifact said `Overall Result: PASS` while its stderr recorded Godot parse/load failures in gameplay scripts. That meant the failure could stay invisible in both future smoke runs and fresh public audits.
- **Evidence**: The retained VA smoke artifact `.opencode/state/artifacts/history/release-001/smoke-test/2026-04-10T11-36-40-894Z-smoke-test.md` contains `Overall Result: PASS` plus `SCRIPT ERROR: Parse Error` and `ERROR: Failed to load script` lines. After the patch, fresh public audit `womanvshorseVA/diagnosis/20260412-033025/` emits `EXEC-GODOT-006` from that contradictory artifact instead of treating the repo as workflow-clean plus follow-up only.
- **Validates**: Future Godot release smoke runs can no longer silently pass through parse/load failures, repo-local closeout cannot trust contradictory PASS smoke artifacts, and public audit now surfaces the exact false-finish shape that previously escaped notice.

### WORKTREE 2026-04-12 — Finish-validation lane backfill for consumer repos
- **What**: Patched `bootstrap_repo_scaffold.py` so consumer-facing repos with explicit Product Finish Contract signals seed `FINISH-VALIDATE-001` and wire `RELEASE-001` to depend on it even when the brief allows procedural or placeholder-friendly content. Patched `audit_contract_surfaces.py` to emit `FINISH003` when an older repo records explicit finish signals but still lacks that finish-validation lane or release dependency, while ignoring generic scaffold placeholder finish-contract text so framework-agnostic starter repos do not get false consumer-facing hits. Patched `apply_repo_process_repair.py` so managed repair now backfills `VISUAL-001` / `AUDIO-001` / `CONTENT-001` as needed plus `FINISH-VALIDATE-001` and the release dependency into older consumer repos, instead of leaving the finish contract as brief-only truth.
- **Why**: The VA false-finish problem was broader than the contradictory smoke artifact. Older consumer repos could carry explicit finish signals in the canonical brief while their ticket graph still let `RELEASE-001` close without any dedicated finish-validation owner. That made finish truth advisory instead of executable.
- **Evidence**: Fresh public audit `womanvshorseVA/diagnosis/20260412-034402/` emits `FINISH003` before repair, proving the missing-lane defect is observable. The next managed repair cycle backfilled `FINISH-VALIDATE-001` (plus finish ownership tickets) into `womanvshorseVA/tickets/manifest.json`, and the follow-up public audit `diagnosis/20260412-034655/` cleared `FINISH003`, leaving only source-follow-up findings (`EXEC-GODOT-006`, `EXEC-REMED-001`). `spinner` then reproduced the same package defect at `diagnosis/20260412-034826/`; managed repair backfilled `FINISH-VALIDATE-001` into `spinner/tickets/manifest.json`, and the follow-up public audit `diagnosis/20260412-035103/` cleared `FINISH003` there as well.
- **Validates**: Scafforge no longer treats finish-contract enforcement as brief-only metadata for older consumer repos; the managed repair path can now retrofit the missing finish-validation lane and make public audit converge off the structural gap in repeated downstream repair loops.

### WORKTREE 2026-04-12 — Weak interactive finish-contract hardening
- **What**: Patched `bootstrap_repo_scaffold.py` so interactive/mobile/game scaffolds replace placeholder or older generic finish signals with stronger user-observable interaction-proof requirements, and seed `FINISH-VALIDATE-001` with an explicit interaction-evidence acceptance clause. Patched `audit_contract_surfaces.py` to emit new finding `FINISH004` when an interactive consumer repo still uses Scafforge-generated generic `finish_acceptance_signals` or a `FINISH-VALIDATE-001` ticket that does not require explicit interaction evidence. Patched `apply_repo_process_repair.py` so managed repair upgrades older generated finish-contract prose and rewrites existing `FINISH-VALIDATE-001` acceptance to the stronger interaction-proof contract. Updated `shared_verifier.py` and `scripts/smoke_test_scafforge.py` to lock the new audit/repair behavior into package validation.
- **Why**: The missing finish-validation lane was only part of the false-completion problem. Spinner still showed that Scafforge’s own older generated finish bar for interactive repos was too generic to prove a real shipped interaction loop, so a repo could clear the structural lane while still carrying a weak machine-generated “finished enough” story.
- **Evidence**: Spinner’s canonical brief still recorded the exact old generated toddler/toy finish signal (`docs/spec/CANONICAL-BRIEF.md` line 121), and `FINISH-VALIDATE-001` still lacked any explicit interaction-evidence acceptance. Fresh public audit `spinner/diagnosis/20260412-040413/` emitted `FINISH004`. Managed repair then upgraded spinner’s brief and ticket graph (`docs/spec/CANONICAL-BRIEF.md` line 121 now requires the toy-box flow to be reachable with immediate visible feedback; `tickets/manifest.json` lines 5093-5094 now require user-observable interaction evidence). Follow-up repair verification converged on the same cycle (`diagnosis/20260412-041729/`), and fresh public audit `spinner/diagnosis/20260412-041905/` cleared `FINISH004`, `FINISH003`, and `SKILL001`, leaving only `EXEC-REMED-001` source follow-up.
- **Validates**: Scafforge now treats weak machine-generated interactive finish contracts as a repairable package defect instead of acceptable finish truth. Interactive repos must carry explicit interaction-proof language in both the canonical brief and `FINISH-VALIDATE-001`, and managed repair can retrofit older repos onto that stronger bar.

### WORKTREE 2026-04-12 — Blender-agent validation-contract hardening
- **What**: Patched `blender-agent/mcp-server/src/blender_mcp_server/config.py` so Windows absolute roots from env files normalize correctly even on non-Windows hosts, restored live-session `client_type` metadata in `live_session.py`, added failure-class alias handling in `live_session_integration.py`, repaired schema-definition resolution and public naming in `schema_validator.py`, converted `tests/test_runtime_worker_persistence.py` into a real `unittest` module with explicit `src/` bootstrapping, declared pytest as a test extra in `mcp-server/pyproject.toml`, updated the validation guidance in `mcp-server/README.md`, and aligned `tests/test_watch_mode.py` with the actual SSE rendering contract.
- **Why**: Real host validation showed the Blender-backed integration suite was already healthy, but the repo’s broader validation contract had drifted in several places: Windows env-file roots broke on Linux, live-session tests had lost metadata/recovery compatibility, one runtime persistence file was pytest-shaped while being invoked through `unittest`, schema validation wrapped `$ref` definitions incorrectly, and the documented “full suite” command did not match the repo’s actual pytest-backed test surface.
- **Evidence**: `python3 -m unittest tests.test_runtime_worker_persistence tests.test_blender_integration -v` initially failed on `ModuleNotFoundError: blender_mcp_runtime`, while the real Blender-backed integration tests all passed against `/home/pc/blender-4.5.0/blender`. The broader suite then exposed concrete contract failures: `test_config` mis-normalized `C:\...` roots into Linux-relative paths, `test_live_session` had lost `metadata.client_type` and alias compatibility for `transport_lost`, `test_schema_redesign` failed with invalid `$ref` wrapper resolution and missing public `blender_session_create` schema names, and `test_watch_mode` still expected SSE clients to receive flat JSON instead of `event/data`.
- **Validates**: Blender-agent now has a coherent local validation story that matches its real execution surface instead of a half-unittest / half-pytest mismatch, while preserving the already-working real Blender-backed tool contracts.

### WORKTREE 2026-04-12 — Remediation-review verdict compatibility + non-recursive Kilo repair
- **What**: Patched repo-template `.opencode/lib/workflow.ts` and `skills/scafforge-audit/scripts/audit_lifecycle_contracts.py` so remediation-review evidence accepts markdown verdict sections like `## Verdict` followed by `**PASS**`, not just inline `Overall Result: PASS` forms. Tightened the reviewer prompt in `__AGENT_PREFIX__-reviewer-code.md` to emit the canonical inline result as well, added smoke coverage in `scripts/smoke_test_scafforge.py`, and then hardened both `scripts/run_agent.sh` and `skills/scafforge-repair/SKILL.md` so Kilo-backed repair passes may not recurse into `./scripts/run_agent.sh <repo> --repair` from inside an already-running repair cycle.
- **Why**: A real spinner remediation child (`REMED-021`) generated a valid markdown verdict that the downstream review→QA gate still rejected, exposing a prompt/gate/audit contract mismatch. The first attempt to propagate that fix through Kilo repair then exposed a second host-side defect: Kilo was willing to call the public repair runner recursively instead of staying on the underlying managed-repair scripts.
- **Evidence**: Spinner’s current review artifact `.opencode/state/reviews/remed-021-review-review.md` contained explicit command evidence plus a heading-style verdict (`## Verdict` / `**PASS**`) but was blocked by the old strict regex. The prior repair trace (`active-plans/agent-logs/spinner-repair-2026-04-12T05-58-48.log`) showed Kilo re-entering `run_agent.sh spinner --repair`. After the patch, fresh auto repair `active-plans/agent-logs/spinner-repair-2026-04-12T06-06-24.log` fell through Codex exhaustion into Kilo, stayed on `run_managed_repair.py`, recorded `project-skill-bootstrap` and `ticket-pack-builder`, reconciled `managed_blocked -> source_follow_up`, and completed without nested runner recursion. The follow-up live `opencode` run `active-plans/agent-logs/spinner-opencode-2026-04-12T06-08-36.log` then closed `REMED-021` and advanced into `REMED-022`, proving the original remediation-review gate blocker no longer traps execution.
- **Validates**: Scafforge now accepts the remediation-review verdict shape it actually told downstream reviewers to write, public audit matches the same rule, and Kilo fallback repair stays on the direct managed-repair path instead of nesting the host runner when Codex quota is exhausted.

### WORKTREE 2026-04-12 — Session-backed coordinator-artifact enforcement
- **What**: Patched repo-template `.opencode/plugins/stage-gate-enforcer.ts` so planning, plan-review, implementation, review, and QA artifacts cannot be written or registered by the `*-team-leader` coordinator. Because live `tool.execute.before` calls were arriving with `agent: null`, also added `resolveAgentNameFromSession()` in `.opencode/lib/workflow.ts` so the stage gate can recover the current agent from `.opencode/state/invocation-log.jsonl` using `sessionID`. Updated the smoke harness (`scripts/test_support/scafforge_harness.py`) and `scripts/smoke_test_scafforge.py` to simulate the real `sessionID-with-null-tool-agent` runtime shape and lock the regression down.
- **Why**: A resumed spinner run showed the coordinator still writing planning, review, and QA artifacts directly in live execution even though the team-leader prompt explicitly forbids coordinator-authored stage artifacts. The first stage-gate patch looked correct in harness but did not fire downstream because the real tool hook did not populate `input.agent`; only the earlier `chat.message` event in the same session still carried the agent name.
- **Evidence**: `spinner-opencode-2026-04-12T06-24-31.log` shows `spinner-team-leader` calling `artifact_write` / `artifact_register` for `REMED-023` review and QA bodies directly. Spinner’s invocation log confirmed the root cause: `chat.message` events carried `agent: "spinner-team-leader"` while `tool.execute.before` events for the same session recorded `agent: null`. After the session-backed resolver patch, Copilot repair `spinner-repair-2026-04-12T06-55-32.log` propagated the new gate and fresh live execution `spinner-opencode-2026-04-12T07-03-46.log` hit the expected blocker — `artifact_write` by the coordinator is now rejected with `Coordinator-authored planning artifacts are not allowed.` — and the team leader switched to the planner/implementer specialists for `REMED-026`.
- **Validates**: Scafforge no longer relies on prompt compliance alone to keep coordinators from fabricating specialist artifacts. The downstream gate now enforces specialist-owned planning/implementation/review/QA artifact authorship against the same runtime metadata shape that real opencode sessions emit.

### WORKTREE 2026-04-12 — QA verdict-family parser hardening
- **What**: Patched repo-template `.opencode/lib/workflow.ts` so the shared artifact verdict extractor accepts the full QA/review verdict family already present in downstream artifacts: compact stage headings like `## QA PASS`, inline `QA Result: PASS` / `Review Result: APPROVE`, and plain `**Overall**: PASS` labels inside result sections. Expanded `skills/scafforge-audit/scripts/audit_lifecycle_contracts.py` and `audit_reporting.py` so public audit now treats those shapes as part of the same `WFLOW026` contract class instead of only the older markdown-emphasized-label case. Added smoke coverage in `scripts/smoke_test_scafforge.py` for compact QA headings and `**Overall**: PASS` QA summaries.
- **Why**: Fresh spinner execution hit another live verdict mismatch immediately after the coordinator-authorship fix: `REMED-027` completed QA and passed `smoke_test`, but `ticket_update` still rejected the QA→smoke-test transition with `Cannot advance past qa — latest artifact verdict is unclear.` The live QA artifact was explicit, but it used a compact heading/result combination the shared parser still did not understand.
- **Evidence**: `spinner/.opencode/state/qa/remed-027-qa-qa.md` records `## QA PASS — Both Acceptance Criteria Satisfied` plus `## QA Result` / `**Overall**: PASS`, while the live run `active-plans/agent-logs/spinner-opencode-2026-04-12T07-03-46.log` still failed the stage update after a passing smoke test. Existing downstream artifacts proved the shape family was already real and recurring: `spinner/.opencode/state/qa/remed-014-qa-qa.md` uses `## QA PASS`, `spinner/.opencode/state/qa/bugfix-nav-003-qa-qa.md` uses `## QA Result: PASS`, and `womanvshorseVD/.opencode/state/qa/remed-002-qa-qa.md` uses `**Overall**: PASS pending smoke test confirmation.`
- **Validates**: Scafforge no longer blocks QA→smoke-test progression on explicit verdicts just because the artifact uses compact QA/review headings or plain `Overall` result labels. The same shared extractor now covers live repo behavior, public audit detection, and local smoke validation.

### WORKTREE 2026-04-12 — WFLOW026 post-package routing repair
- **What**: Patched `skills/scafforge-audit/scripts/audit_reporting.py` so `package_has_verdict_parser_fix(...)` recognizes the widened QA/review verdict parser already installed in the package, instead of only the older markdown-label surface. Updated the smoke suite to assert the `WFLOW026` route through `build_ticket_recommendations(...)` directly, which is the real recommendation path used to decide whether a fresh diagnosis should go to `scafforge-repair` or remain `manual-prerequisite`.
- **Why**: The parser fix itself was real, but fresh spinner diagnosis packs still marked `WFLOW026` as `package_work_required_first`. That trapped managed repair behind a false prerequisite even though the installed package already contained the fix. The bug was not in the parser anymore; it was in the audit recommendation detector that decides whether the fix exists in-package.
- **Evidence**: Fresh spinner diagnosis `spinner/diagnosis/20260412-063454/` still classified `WFLOW026` as `manual-prerequisite`, and `run_managed_repair.py` correctly refused to proceed from that diagnosis. Direct module checks then showed `package_has_verdict_parser_fix(...)` can now evaluate true against the current package and `build_ticket_recommendations(...)` now emits `route: scafforge-repair` for `WFLOW026` once the widened parser is present.
- **Validates**: Fresh post-package audits can now classify the verdict-parser defect as repairable Scafforge-managed surface work instead of a stale package-first blocker, which unblocks the next spinner audit → repair propagation cycle.

### c7546b95 — Template opencode.jsonc + CONFIG audit
- **What**: Added model, default_agent, external_directory, bash allowlist to template opencode.jsonc. Created audit_config_surfaces.py with CONFIG001-005 checks. Wired into audit and disposition routing.
- **Why**: Downstream repos missing critical config fields. Audit had no way to detect config problems.
- **Evidence**: All 3 repos had incomplete opencode.jsonc; agents couldn't start properly.
- **Validates**: Repair now generates correct config; audit catches missing fields.

### 906dc002 — RC-001, RC-002, RC-005 fixes
- **What**: Added managed_blocked enforcement to stage-gate-enforcer (RC-001). Stopped injecting "deterministic-refresh" into completed_stages (RC-002). Added WFLOW031 anti-pattern guidance to team-leader template (RC-005).
- **Why**: managed_blocked was declared but not enforced; deterministic-refresh polluted stage tracking; agents fell into repair prediction loops.
- **Evidence**: Root cause analysis of agent loops and stuck states.
- **Validates**: Agents now blocked when they should be; stage tracking clean; team leader warned about repair traps.

### 03adb917 — Repair provenance round-trip + pivot normalization + audit depth
- **What**: Fixed load_metadata() to recover stack_label and product_finish_contract from bootstrap-provenance.json. Added pivot state normalization. Added model format and CONFIG005 audit checks.
- **Why**: Repair rendered "framework-agnostic" everywhere instead of actual stack labels. Pivot state inconsistencies blocked handoff.
- **Evidence**: Repair output showed wrong stack labels; provenance had correct data but wasn't being read.
- **Validates**: Repair now renders correct project-specific values.

### 6e9bb3dc — Exclude .venv/node_modules from copytree
- **What**: Added .venv*, node_modules, __pycache__ to shutil.copytree ignore patterns in repair.
- **Why**: .venv contains circular symlinks that crash copytree; node_modules and __pycache__ are never repair targets.
- **Evidence**: Repair crashed on GPTTalker with shutil.copytree error.
- **Validates**: Repair completes on repos with virtual environments.

### 7ead92e0 — Strip provider prefix from provenance model names
- **What**: Before re-rendering template, strip provider prefix from model names stored in provenance.
- **Why**: Provenance stores "provider/model" but template rendering prepends provider again.
- **Evidence**: Rendered output showed "minimax-coding-plan/minimax-coding-plan/MiniMax-M2.7".
- **Validates**: Model references in rendered output are correct.

### aad675cb — Widen venv exclusion pattern
- **What**: Changed .venv to .venv* in copytree ignore to catch .venv310, .venv312, etc.
- **Why**: Some repos use version-specific venv names.
- **Evidence**: Edge case found during testing.

### 686cc74a — CRITICAL: Add missing hasPendingRepairFollowOn import
- **What**: Added import of hasPendingRepairFollowOn to stage-gate-enforcer.ts.
- **Why**: RC-001 enforcement called this function but forgot to import it. Since the plugin runs before ALL tool calls, every single tool invocation crashed.
- **Evidence**: All 3 repos showed "hasPendingRepairFollowOn is not defined" on every tool call.
- **Validates**: Tool calls work again in all downstream repos.

### c8bb368f — Skip REMED-RELEASE-GATE creation when ticket exists
- **What**: Added existence check before creating REMED-RELEASE-GATE in apply_remediation_follow_up.py.
- **Why**: Re-running repair crashed with "Ticket already exists" at remediation ticket creation step.
- **Evidence**: Spinner and glitch repairs failed at this step.
- **Validates**: Repair is idempotent for ticket creation.

### 9460cc63 — Improve managed_blocked error message
- **What**: Changed BLOCKER message to list allowed tools, mention repair_follow_on_refresh, and explain how to handle host-agent skills.
- **Why**: Old message said "report to operator" — unhelpful for headless agents. Agents got stuck in loops.
- **Evidence**: GPTTalker agent tried artifact_write, task, ticket_claim repeatedly. Spinner agent figured it out by luck.
- **Validates**: Glitch agent immediately used repair_follow_on_refresh after seeing the improved message.

### 3464bd98 — Documentation: architecture.md + scafforge-context.json
- **What**: Added comprehensive architecture documentation and machine-readable context JSON.
- **Why**: Fast agent bootstrapping and human understanding of the system.
- **Evidence**: The package had many moving parts but no single machine-readable context surface.
